/**
 * Constants for ChatInput components
 */

import { ELEMENT_TAG_COLORS } from '@/types/element-reference'

/**
 * Default max height for collapsible panel
 */
export const COLLAPSIBLE_PANEL_MAX_HEIGHT = 'max-h-96'

/**
 * Maximum total IDs to annotate during element selection
 */
export const MAX_TOTAL_IDS = 120

/**
 * Selection timeout in milliseconds
 */
export const SELECTION_TIMEOUT_MS = 30000

/**
 * Image preview size
 */
export const IMAGE_PREVIEW_SIZE = 'h-16 w-16'

/**
 * Truncate length for element text
 */
export const ELEMENT_TEXT_TRUNCATE_LENGTH = 100

/**
 * Truncate length for agentId in compact display
 */
export const AGENT_ID_TRUNCATE_LENGTH = 40

/**
 * Truncate length for queue item text
 */
export const QUEUE_TEXT_TRUNCATE_LENGTH = 40

/**
 * Element tag colors (re-exported for convenience)
 */
export { ELEMENT_TAG_COLORS }
